'use strict';

describe('Controller: ticketController', function () {

    //var ticketController, ticketFactory, scope;

    // load the controller's module
    // beforeEach(function() {
    //     module('ticketApp', []);
    // });

    // // Initialize the controller and a mock scope
    // beforeEach(inject(function ($controller, $injector, $rootScope, $http, $httpBackend) {
    //     scope            = $rootScope.$new();
    //     createFactory    = function() {
    //         return $injector.get('ticketFactory', {
    //             $http: $http,
    //         });
    //     };
    //     createController = function() {
    //         return $controller('ticketController', {
    //             $scope: scope,
    //             $http : $http,
    //             ticketFactory: createFactory(),
    //         });
    //     };
    // }));

    // it('truc', function () {
    //     var mainCtrl = createController();
    // });
});